multiple <- function(A,B){
	nRow <- nrow(A)
	nCol <- ncol(B)
	# empty matrix
	newMat <- matrix(data = NA,
		ncol = nRow,
		nrow = nCol
	)
	for(Row in 1:nRow){
		for(Col in 1:nCol){
			# fill the gap
			# get A
			# get B
			newMat[Row, Col] <- 
		}
	}

}
